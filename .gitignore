/entire-graph
/dist/
__pycache__/
*.py[cod]

# Benchmark working data: cloned external repos must never enter our commits.
/bench/.cache/
# Per-run benchmark reports are local-only working data, not version-controlled.
/bench/results/
# Local graph index cache used while developing (--cache-dir): a rebuildable
# artifact, and large.
/.cache/
# macOS Finder metadata. Never belongs in a commit.
.DS_Store
